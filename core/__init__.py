# This file makes the 'core' directory a Python package.

# You can optionally import key classes/functions here to make them
# available directly from the package, e.g.:
# from .llm_client import get_api_key, generate_text
# from .models import AgentConfig, MarketState, Agent
# from .prompt_manager import get_prompt
# from .simulation_engine import MarketSimulation

# For now, keeping it simple. Users of the package will import specific modules.